import 'package:flutter/material.dart';

class AppLocalization {
  static const String assetPath = 'assets/i18n/generated';
  static const Locale fallbackLocale = Locale('th');
  static const List<Locale> supportedLocales = [
    Locale('th'),
    Locale('en'),
  ];
}
